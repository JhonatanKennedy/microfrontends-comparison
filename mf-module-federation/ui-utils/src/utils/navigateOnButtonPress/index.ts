import { MouseEvent } from "react";
// import { navigateToUrl } from "single-spa";

export function navigateOnButtonPress(e: MouseEvent<HTMLButtonElement>) {
  // TODO: change this when routes come
  // navigateToUrl(e.currentTarget.name);
}
